Java follows PascalNamingConvention for class name and camelCaseConvention for methods name.
Java is case-sensitive. The name of the class and the file name should be same, if not java will give error because java complies the code by searching the class name.
### main()
It is an entry point for the complier to start compiling the source code.
System.out.println()
this means send text to the screen, where system is built-in class, out is a member of System and shortortform of outputand println() is a method to print the line.
Every code is ended by semi colon (;)
